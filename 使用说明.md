# 财务管理系统 · 使用说明

一个**纯本地、数据不出机**的发票识别与审核工具：上传发票 → 自动识别提取 → 人工审核 → 导出 Excel 工作底稿。
所有数据只留在你自己的电脑上，**不上传云端、不连任何外部服务**，服务只监听本机 `127.0.0.1`。

---

## 一、运行要求

- 需要电脑已安装 **Python 3**（macOS 建议 3.10+，Windows 安装时请勾选 *Add Python to PATH*）。
  - 下载：https://www.python.org/downloads/
- 首次启动需联网安装依赖（之后可离线使用）。
- **可选**：安装 **LibreOffice**（免费，https://www.libreoffice.org/download/ ）以支持旧版 **.doc/.xls/.ppt、RTF、ODF** 等格式的自动识别；不装也能用（这类文件可下载原件人工录入，或另存为 .docx/PDF）。启动时若未检测到会给出提示。

## 二、启动（双击即可）

- **macOS**：双击 **`启动.command`**
  - 若提示"无法打开，因为来自身份不明的开发者"：在该文件上 **右键 → 打开 → 打开**（仅首次需要）。
- **Windows**：双击 **`启动.bat`**

**首次启动会自动创建运行环境并一次性装齐全部依赖（含 OCR）**，随后**自动打开浏览器**进入系统。
- OCR 依赖体积较大，**首次安装需联网、可能几分钟到十几分钟**，请耐心等待(窗口里能看到进度)。
- 以后再启动会直接进入，很快。
- 无需任何单独的"安装 OCR"步骤——扫描件/图片发票开箱即用。

> 关闭那个黑色/终端窗口，或在窗口里按 **Ctrl+C**，即停止服务。数据全部留在本机。

## 三、怎么用

1. **上传**：把发票文件拖进首页（或点击选择），支持 **PDF / Word(.docx) / Excel(.xlsx) / 图片**，可多选。系统自动识别字段、金额、服务明细，并做内部校验与风险评分。
   - **旧版 .doc/.xls/.ppt、RTF、ODF 等**：若本机装了 **LibreOffice** 会自动转换后识别；未装则该文件进队列后可**下载原件人工录入**，或把它**另存为 .docx / PDF** 再上传即可自动识别。
2. **审核**：进入审核页逐张核对。
   - 左侧显示**原件**，右侧是提取出的字段；**点字段 → 原件高亮对应位置**，**点原件蓝框 → 高亮对应字段**（PDF / Word / Excel 均支持）。
   - 可**直接修改**任何字段、服务明细（增/删/改/✂拆分），改动**留痕可追溯**。
   - 可在原件上**框选**一段文字 → 复制或填入某字段（填入后该字段也能高亮对应）。
   - **疑似重复**发票可一键进入左右对比逐项确认；审核前的脏数据/测试记录可删除。
3. **确认**：核对无误点 **Approve**（通过）。系统**绝不自动入账**，必须人工确认。
4. **对账确认（发票 ↔ 银行流水）**：上传时可切「🏦 银行流水」识别对账单/流水；进入 **🔗 对账确认** 页点「▶ 运行自动匹配」，系统按发票号/金额/币种/供应商/日期自动把发票和流水配上：
   - **高可信且唯一**的匹配进「待批量确认」，可**一键全部通过**；中等/一票多付/多票合并付/有差额的进「待确认」逐组核对；多候选进「重点审核」；没配上的进「待定」。
   - **没通过自动校验的记录**（缺关键字段、疑似重复等）会提示先去审核页纠错，不参与匹配。
   - 一次确认「提取信息 + 对应关系」，不用再拆成多次审核。
5. **导出 Excel**：回首页点导出。
   - **导出全部**：导出库内所有记录的最新值（改完即生效，无需先 Approve）。
   - **仅导出已通过**：只导出已 Approve 的记录（把审核当入账闸门）。

## 四、隐私与安全

- 全部本地处理，**数据不出机**；服务仅监听 `127.0.0.1`，不对外开放。
- 不调用任何云端 / 外部 AI 服务；识别与校验均为本地规则。
- 你上传的发票、数据库都在本工具目录下的 `data/` 内，**不随软件分发**。用完关闭窗口即停。

## 五、常见问题

- **双击没反应 / 一闪而过**：多半是没装 Python 3，或 Windows 没勾选 *Add Python to PATH*。装好后重试。
- **首次启动很久**：正在装依赖（含体积较大的 OCR），需联网、可能十几分钟，窗口里有进度，正常。
- **浏览器没自动打开**：手动访问启动窗口里显示的地址（形如 `http://127.0.0.1:8000`）。
- **扫描件识别不出**：多为首次 OCR 依赖没装全（网络中断等）——重新双击启动会自动补装；文本型 PDF/Word/Excel 不受影响。
- **端口被占用**：系统会自动换一个可用端口，以启动窗口里显示的地址为准。

## 六、想了解系统本身

随包附带 `wiki/` 知识库（纯 markdown，可用任意编辑器或 Obsidian 阅读），介绍架构、审核与风险语义、安全基线、各类发票输入格式等。入口：`wiki/index.md`。
